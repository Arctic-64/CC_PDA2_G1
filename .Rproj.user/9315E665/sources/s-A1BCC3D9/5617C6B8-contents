librarys
```{r}
library(shiny)
library(tidyverse)
library(ggplot2)
library(here)
library(lubridate)
library(sf)
library(leaflet)
```

data loading
```{r}
raw_data <- read_csv(here("data/beds_by_nhs_board_of_treatment_and_specialty.csv"))
hospitals <- read_csv(here("data/hospitals scotland.csv"))
```

addition of geo data

```{r}
data <- inner_join(raw_data, hospitals, by = "Location")
scotland
```

data preprocessing
```{r}

```

```{r}
```


```{r}
```

```{r}
```

```{r}
```

